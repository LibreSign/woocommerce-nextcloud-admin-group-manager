{
	"name": "woocommerce-nextcloud-admin-group-manager",
	"private": true,
	"license": "GPL-2.0-or-later",
	"description": "Browser tests for the WooCommerce Nextcloud Admin Group Manager",
	"scripts": {
		"test:e2e": "playwright test",
		"test:e2e:ui": "playwright test --ui",
		"env:start": "docker compose -f tests/E2E/docker-compose.yml up -d --wait && docker compose -f tests/E2E/docker-compose.yml run --rm cli",
		"env:stop": "docker compose -f tests/E2E/docker-compose.yml down -v"
	},
	"devDependencies": {
		"@playwright/test": "^1.63.0"
	}
}
